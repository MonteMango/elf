Build the Elfy project for iOS Simulator.

Run this command:
```bash
xcodebuild -scheme elf -destination 'platform=iOS Simulator,name=iPhone 17 Pro' build
```

If build fails, analyze the errors and fix them.
