Run SwiftLint on the project to check code style.

Run this command:
```bash
swiftlint
```

If there are violations:
1. List all violations grouped by severity (error/warning)
2. For auto-fixable issues, suggest running `swiftlint --fix`
3. For manual fixes, explain what needs to change
