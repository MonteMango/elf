name: iOS starter workflow

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  build:
    name: Build and Test
    runs-on: macos-26

    steps:
      - name: Checkout
        uses: actions/checkout@v5

      - name: Select Xcode
        uses: maxim-lobanov/setup-xcode@v1
        with:
          xcode-version: latest-stable

      - name: Select iOS Simulator
        id: select_device
        run: |
          # Pin a known-good simulator that exists on the macos-26 image.
          # The previous xctrace regex was fragile and resolved to an
          # unmatchable destination on the new image.
          echo "device=iPhone 17" >> $GITHUB_OUTPUT

      - name: Build main app (elf)
        run: |
          if [ "`ls -A | grep -i \\.xcworkspace\$`" ]; then \
            filetype_parameter="workspace" && file_to_build="`ls -A | grep -i \\.xcworkspace\$`"; \
          else \
            filetype_parameter="project" && file_to_build="`ls -A | grep -i \\.xcodeproj\$`"; \
          fi
          file_to_build=$(echo $file_to_build | awk '{$1=$1;print}')
          xcodebuild clean build \
            -scheme elf \
            -"$filetype_parameter" "$file_to_build" \
            -destination "platform=iOS Simulator,name=${{ steps.select_device.outputs.device }}" \
            -derivedDataPath DerivedData

      - name: Build elf_Kit for testing
        run: |
          if [ "`ls -A | grep -i \\.xcworkspace\$`" ]; then \
            filetype_parameter="workspace" && file_to_build="`ls -A | grep -i \\.xcworkspace\$`"; \
          else \
            filetype_parameter="project" && file_to_build="`ls -A | grep -i \\.xcodeproj\$`"; \
          fi
          file_to_build=$(echo $file_to_build | awk '{$1=$1;print}')
          xcodebuild clean build-for-testing \
            -scheme elf_Kit \
            -"$filetype_parameter" "$file_to_build" \
            -destination "platform=iOS Simulator,name=${{ steps.select_device.outputs.device }}"

      - name: Run elf_Kit tests
        run: |
          if [ "`ls -A | grep -i \\.xcworkspace\$`" ]; then \
            filetype_parameter="workspace" && file_to_build="`ls -A | grep -i \\.xcworkspace\$`"; \
          else \
            filetype_parameter="project" && file_to_build="`ls -A | grep -i \\.xcodeproj\$`"; \
          fi
          file_to_build=$(echo $file_to_build | awk '{$1=$1;print}')
          xcodebuild test-without-building \
            -scheme elf_Kit \
            -"$filetype_parameter" "$file_to_build" \
            -destination "platform=iOS Simulator,name=${{ steps.select_device.outputs.device }}"
