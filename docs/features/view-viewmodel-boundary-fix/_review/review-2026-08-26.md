---
status: CHANGES REQUESTED
reviewer: "sdd:reviewer (clean-context, model: opus)"
date: "2026-08-26"
---

# Review record — view-viewmodel-boundary-fix

## Scope

Loop-back re-review после `_review/review-2026-08-24.md` (вердикт `CHANGES REQUESTED`, 7 находок "fix now" + 1 "defer"). Follow-up `implement`-прогон закрыл все 7 находок задачами T14–T20 (см. `pipeline-log.md` § Implement, «Follow-up прогон»). Diff остаётся незакоммиченным рабочим деревом (ветка `feature/structured-task-cancellation` несёт этот фикс поверх себя, отдельной feature-ветки нет) — 17 изменённых файлов (187 insertions / 48 deletions) + 12 новых файлов (`BattleResultCompletionViewModel.swift` + unit/wiring-тесты для T1–T20).

Изменённые: `GameSession.swift`, `BattleFightViewModel.swift`, `DungeonViewModel.swift`, `FarmActivityViewModel.swift`, `GameDayViewModel.swift`, `GameDayStateViewModel.swift`, `GameSession+ViewModelFactories.swift`, `ModalRoute.swift`, `SessionRouteView.swift`, `BattleFightRouteView.swift`, `BattleResultScreen.swift`, `DungeonScreen.swift`, `GameDayScreen.swift`, `PreviewGame.swift`, `.claude/docs/common-mistakes.md`, `.swiftlint.yml`.

Вне scope (принадлежат другой работе в том же рабочем дереве, не ревьюились): `CONTEXT.md`, `nextArch/*`, `Package.resolved`.

Прогнано reviewer'ом: `swiftlint --quiet` (16 pre-existing нарушений, 0 в изменённых строках), `xcodebuild test -scheme elf_Kit` (**540/540 зелёных** XCTest, +4 к прошлому прогону; 14/14 Swift Testing), `xcodebuild -scheme elf build` (success). KPI spec §7 перепроверены: `try? await session.save()` → 0 в продакшн-коде; прямые мутации из View → 0 (baseline 5 сайтов закрыты; 2 оставшихся упоминания — `AppCoordinator` (координатор, не View) и `DungeonScreen.swift:166` внутри `#if DEBUG #Preview` (V-7, вне scope)).

## iOS-консультанты (ADR-0002/ADR-0005)

AND-gate (spec-visible ∧ diff-visible): UI-класс и async-класс сработали (не изменилось с прошлого ревью). Оба консультанта пре-консультированы параллельно, scoped к диффу T14–T20, их брифы вплетены в dispatch reviewer'а на quality-bar altitude и подтверждены им независимо (см. ниже).

- **SwiftUI-консультант** — пре-консультирован на диффе T15/T16/T19 (`GameDayScreen`, `GameDayViewModel`, `SessionRouteView`, `ModalRoute`, `BattleResultScreen`). Подтвердил оба фикса solid, без новых lifecycle/`@State`/`.task{}` регрессий; одна info-заметка про `BattleResultScreen.continueAfterBattle()` (вне scope этих задач, не регрессия).
- **Swift-concurrency-консультант** — пре-консультирован на диффе T14/T17/T18 (`BattleFightViewModel.finishBattle()`, `BattleFightRouteView`, `GameSession`, `exitGame()`/`advanceToNextDay()`). Подтвердил: переупорядоченное тело `finishBattle()` не содержит ни одной точки приостановки — гарантия порядка структурная, не просто дисциплина вызовов; dungeon-скоуп T18 race-free (проследил, что `concludeRoomBattle`/`applyBattleOutcome` не трогают `GameSession.dungeonSession`).

## Чек-лист прошлого ревью (9 пунктов) — все закрыты

Полная сверка находок `review-2026-08-24.md` против кода (см. полный текст в результате диспетчеризации reviewer'а, свёрнуто здесь для краткости): все 7 "fix now" (T14, тест T14, T15/T16, T16 orphan, T17, T18, T19, T20) подтверждены исправленными по коду и тестам; 1 "defer" (T11 / отсутствие схемы `elf_iOS`) честно остаётся в spec §8, не потеряно.

## Трассировка US → AC → flow → task → код + тест (полный проход)

Все AC-01…AC-07 (включая AC-02b/AC-06b) и все US-01…US-05 прослежены end-to-end без разрывов; регрессий в поверхности T1–T13 не найдено. AC-04 — самая пострадавшая находками AC в этом цикле (см. находки #1/#2 ниже), но по коду и тестам после T14/T18 корректна.

## Находки и резолюция

- **[stage-1] Тест T14 не выполняет собственный DoD — проверяется порядок вызовов, а не заявленный бизнес-исход «награда роковой комнаты попала в banked total»** — `Packages/elf_Kit/Tests/elf_KitTests/UILayer/BattleFight/BattleFightViewModel_DeathRewardBankingTests.swift:242-281`; AC: AC-04; problem: DoD задачи T14 (`tasks.json`) требует launcher, который «adds a known reward to `pendingRewards`» и тест, который «proves that reward is included in the banked total». Фактический fake-launcher только пишет в `CallSequenceLog` и возвращает `nil`; `SpyDungeonLifecycleMutator.bankDungeonRewardsOnDeath()` — пустая заглушка, инкрементирующая счётчик. Регрессию порядка тест реально ловит (обратный порядок дал бы `["bank","launcher","save"]`), но сам факт «награда попала в банк» нигде не наблюдаем — тест доказывает букву находки #1 прошлого ревью по доверенности, не по существу. verdict: **CONFIRMED**. → **Fix now** (резолюция пользователя: исправить). Suggested: переписать launcher в этом тесте на настоящий `DungeonSession.concludeRoomBattle(...)` через реальный `DefaultDungeonLifecycleMutator`, ассертить прирост `session.state.player.currentExp`/инвентаря после `finishBattle()`.

- **[stage-1] `sad.md` §6 Flow 3 документирует уже исправленный этим циклом баг** — `docs/features/view-viewmodel-boundary-fix/sad.md:189-209`; AC: AC-04; problem: диаграмма не показывает вызов launcher'а вообще и рисует `bankDungeonRewardsOnDeath()` первым шагом после `determineBattleOutcome()`, а `saveInBackground()` — безусловным. Реализация (после T14/T18) делает обратный порядок (launcher → bank → save) и скоупит save на `dungeonSession != nil`. Именно порядок «launcher раньше bank» был предметом находки #1 прошлого ревью — то есть до этой находки sad.md документировал версию, которую сам же review-цикл признал дефектной. verdict: **CONFIRMED**. → **Fix now** (резолюция пользователя: обновить диаграмму). Suggested: обновить mermaid Flow 3 (участник `BattleFightRouteView.onBattleConcluded`/`concludeRoomBattle` перед веткой «герой повержен», dungeon-скоуп на save) и заодно мелкое расхождение порядка `spendActionPoints`/`startDungeonSession` в Flow 1 (`sad.md:154-155` vs `GameDayViewModel.swift:91-92`, info-уровня, безобидно).

- **[stage-2] Doc-комментарий `finishBattle()` содержит два неточных утверждения** — `Packages/elf_Kit/Sources/UILayer/BattleFight/BattleFightViewModel.swift:373`; AC: n/a; problem: «No-op for non-hunt battles (`session == nil`)» неверно дважды — (а) `session == nil` это dev-BattleSetup-поток, а не «не-hunt бои» (у dungeon-боёв тоже есть session, и они не no-op); (б) метод и при `session == nil` не no-op — launcher всё равно вызывается и `battleResult` всё равно присваивается (на этом держится авто-закрытие dev-экрана). verdict: **CONFIRMED**. → **Fix now** (резолюция пользователя: исправить). Suggested: «Skips banking/saving entirely for the dev BattleSetup flow (`session == nil`); the launcher call and `battleResult` assignment still happen».

- **[stage-2] Тесты AC-02/AC-02b не ассертят возвращаемое значение `startDungeonRun()` — именно тот контракт, на который T15 посадил навигацию `GameDayScreen`** — `Packages/elf_Kit/Tests/elf_KitTests/UILayer/GameDay/GameDayViewModel_StartDungeonRunTests.swift:94,131-137,149-155`; AC: AC-02, AC-02b; problem: тестовый хелпер отбрасывает результат (`@discardableResult`), обе no-op-проверки ассертят только `session.dungeonSession == nil` и неизменный AP. Гипотетический регресс «no-op-путь всё равно вернул непустой кортеж» прошёл бы весь пакет зелёным, но увёл бы Игрока на экран подземелья без реально начатого забега. verdict: **CONFIRMED**. → **Fix now** (резолюция пользователя: исправить). Suggested: сделать хелпер возвращающим результат, `XCTAssertNil(result)` в обоих no-op-тестах, `XCTAssertEqual(result?.dungeonId, dungeon.id)` в AC-01-тесте.

- **[stage-2, не блокирующее] `BattleFightRouteView` по-прежнему содержит два прямых вызова facade-мутаторов `DungeonSession`/`GameSession` из launcher-замыкания во View** — `Packages/elf_iOS/Sources/Navigation/RouteViews/BattleFightRouteView.swift:31,40`; AC: spec §2 Goal 1 / §6 NFR row 1 / sad.md §10 QG-1; problem: широкая формулировка Goal 1/NFR-1 требует «0 мест … включая `BattleFightRouteView`», а более узкий критерий design (`sad.md` §4 п.2 / ADR-0001, «мимо фасадных методов») этими двумя вызовами не нарушается, т.к. `concludeRoomBattle`/`concludeHuntBattle` сами фасадные методы. Прошлый review этого не флагал, baseline §7 KPI (5 сайтов) их тоже не включал. Расхождение формулировок, не функциональный баг. verdict: **CONFIRMED** (как расхождение артефактов). → **Defer** (резолюция пользователя) — записано в spec §8 (запись от 2026-08-26) с owner+due.

## Независимая оценка наблюдений консультантов (не вынесены на резолюцию — согласился с обоими без действия)

- `BattleResultScreen.continueAfterBattle()` читает `session.dungeonSession` (`BattleResultScreen.swift:107`) — согласен с SwiftUI-консультантом: это чтение (не мутация), не тот класс связанности, что убрал T15, и вне scope этого фикса (файл не тронут T15/T16/T19). Не находка.
- Дублирование дефолта `{ $0.pop() }` в `SessionRouteView.swift:29,34` — согласен: копирует существующий стиль. Не находка.
- `bank`/`save` как независимые `if` в `finishBattle():392-397` — согласен с concurrency-консультантом: намеренно, соответствует doc-комментарию `GameSession.swift:454-458`. Не находка.

## Вердикт

**CHANGES REQUESTED** — все 7 находок прошлого ревью закрыты по существу; 4 новые находки этого цикла отправлены на `implement` (fix now: T14-тест, sad.md Flow 3, doc-комментарий `finishBattle()`, тесты AC-02/AC-02b), 1 (остаточные facade-вызовы в `BattleFightRouteView`) отложена в spec §8. Сборка/тесты/линт зелёные (540/540 + 14/14, 0 новых lint-нарушений), рантайм-дефектов не осталось — все 4 находки этого цикла тестовые/документационные, ни одна не про сломанное поведение. После фиксов — повторное ревью изменённой поверхности (тест T14, `sad.md` Flow 3, doc-комментарий `finishBattle()`, тесты AC-02/AC-02b).
