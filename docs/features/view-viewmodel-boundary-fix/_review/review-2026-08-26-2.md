---
status: PASS
reviewer: "sdd:reviewer (clean-context, model: opus, effort: high)"
date: "2026-08-26"
---

# Review record — view-viewmodel-boundary-fix (round 3)

## Scope

Третий loop-back ре-ревью после `_review/review-2026-08-26.md` (вердикт `CHANGES REQUESTED`, 4 находки "fix now"/"defer"). Follow-up прогон закрыл все 4 находки задачами T21–T24 (см. `pipeline-log.md` § Implement, «Follow-up прогон (4 задачи T21–T24)»). Diff остаётся незакоммиченным рабочим деревом (ветка `feature/structured-task-cancellation`) — тот же периметр, что и в предыдущем цикле (16 изменённых + новые файлы под `Packages/elf_Kit/Sources/UILayer/BattleResult/`, `Packages/elf_Kit/Tests/`, `Packages/elf_iOS/Tests/`).

Вне scope (принадлежат другой работе в том же рабочем дереве, не ревьюились): `CONTEXT.md`, `nextArch/*`, `Package.resolved`.

## iOS-консультанты (ADR-0002/ADR-0005)

AND-gate (spec-visible ∧ diff-visible), пересчитан на дифф T21–T24: UI-класс и async-класс сработали (не изменилось с прошлого ревью); test-strategy-класс — нет (spec-visible сигнал в §3 non-goals негирован — «миграция XCTest→Swift Testing вне scope», это отрицание собственной поверхности фичи, не утвердительный сигнал).

- **SwiftUI-консультант** — пре-консультирован на `BattleFightViewModel.finishBattle()` (T23), `GameDayViewModel.startDungeonRun()` (T15/T24), `GameDayScreen`'s потребление возвращаемого значения. **`SWIFTUI_CONSULTANT: no admissible item for this scope`** — doc-комментарий соответствует коду, `@discardableResult`/возврат корректны, никакого View-side дублирования состояния.
- **Swift-concurrency-консультант** — пре-консультирован на T21-тесте (`BattleFightViewModel_DeathRewardBankingTests.swift`, реальные `DungeonSession.concludeRoomBattle`/`DefaultDungeonLifecycleMutator`). Вернул один item про неявную зависимость теста от того, что non-detached `Task {}` внутри `saveInBackground()` копирует task-locals при создании (если когда-либо станет `.detached`, `withDependencies`-скоуп теста перестанет применяться незаметно для сборки). **Independently отклонён диспетчеризованным reviewer'ом**: три задействованные зависимости (`dungeonRewardCalculator`/`dropService`/`progressionService`) потребляются синхронно внутри `operation:`-замыкания до единственной точки приостановки; тело самого `Task` (`GameSession.swift:430-441`) трогает только stored-свойства, захваченные при `init` (`gameRepository`, `debugGameLogger`), плюс чистые `state.snapshot()`/`dungeonSession?.resumableSaveData()` — переход на `.detached` не изменил бы ни один ассерт этого теста. Предложенный комментарий задокументировал бы несуществующий контракт. Согласен с обоснованием reviewer'а — не находка.

## Чек-лист прошлого ревью (4 находки round-2 + 1 defer) — все закрыты

Диспетчеризованный reviewer перепроверил каждую по фактическому коду (не по трекеру):

- **Finding 1 (T21)** — `BattleFightViewModel_DeathRewardBankingTests.swift:267-323`: `testFinishBattle_HeroDowned_InvokesLauncherBeforeBanking` теперь гоняет реальный `DungeonSession.concludeRoomBattle(...)` через реальный `DefaultDungeonLifecycleMutator`, ассертит `session.state.player.currentExp == 500` и `pendingRewards == .empty` — порядко-чувствительность настоящая (обратный порядок дал бы `currentExp == 0`). **Закрыто по существу.**
- **Finding 2 (T22)** — `sad.md:189-213` Flow 3: launcher теперь показан до ветки «герой повержен», bank после launcher, save скоуплен на `dungeonSession != nil`; заодно поправлен мелкий разнобой порядка в Flow 1 (`spendActionPoints`→`startDungeonSession`, совпадает с `GameDayViewModel.swift:92-93`). **Закрыто.**
- **Finding 3 (T23)** — `BattleFightViewModel.swift:372-374`: «No-op for non-hunt battles» заменено на точную формулировку про dev BattleSetup-поток. **Закрыто.**
- **Finding 4 (T24)** — `GameDayViewModel_StartDungeonRunTests.swift:90-99,115,119,142,161`: хелпер возвращает результат, оба no-op-теста ассертят `nil`, AC-01-тест ассертит `result?.dungeonId`. **Закрыто.**
- **Finding 5 (defer)** — по-прежнему записана в `spec.md:168` с owner (Vitalii Lytvynov) + due. Не потеряна.

## Трассировка US → AC → flow → task → код + тест (полный проход)

Все AC-01…AC-07 (включая AC-02b/AC-06b) и все US-01…US-05 прослежены end-to-end без разрывов диспетчеризованным reviewer'ом (таблица AC→Flow→код→тест в его отчёте). KPI §7 перепроверены: `try? await session.save()` в продакшн-коде — 0; прямые мутации session из View — 0 (baseline 5 сайтов закрыты; остались `AppCoordinator.swift:62,68` — координатор, не View — и `DungeonScreen.swift:166` внутри `#if DEBUG #Preview`, V-7 вне scope).

## Находки и резолюция (этот цикл)

- **[stage-2] Release-сборка не компилируется — `#Preview`-блоки `BattleResultScreen` не под `#if DEBUG`, но ссылаются на DEBUG-only `PreviewGame`** — `Packages/elf_iOS/Sources/Screens/BattleResultScreen/BattleResultScreen.swift:177,211,230` (было, до фикса); AC: n/a (регрессия сборки, внесена T11); problem: три `#Preview`-блока не были обёрнуты в `#if DEBUG`, а T11 добавил в них `session: PreviewGame.createMockSession()` — символ, объявленный только под `#if DEBUG` (`PreviewGame.swift:12`). Подтверждено прогоном `xcodebuild -configuration Release build` → `BUILD FAILED`, `cannot find 'PreviewGame' in scope` ×3 — до этого диффа ссылки не было (`git show HEAD:…/BattleResultScreen.swift` не содержит `PreviewGame`). Все остальные 11 экранов в `Packages/elf_iOS/Sources/Screens/` оборачивают свои `#Preview` в `#if DEBUG`/`#endif` — `BattleResultScreen` был единственным исключением. verdict: **CONFIRMED**. → **Fix now** (резолюция пользователя). **Применено**: `#Preview`-блок (`BattleResultScreen.swift:176-250`) обёрнут в `#if DEBUG`/`#endif`, как в `DungeonScreen.swift:151/183`. Release build перепрогнан — `BUILD SUCCEEDED`.
- **[stage-2, не блокирующее] Комментарий на пути гибели героя приписывал банковку `BattleFightRouteView`, откуда её убрал этот же дифф** — `BattleResultScreen.swift:113-115` (было); AC: n/a; problem: после T5/T12 банковка живёт исключительно в `BattleFightViewModel.finishBattle()`, а `BattleFightRouteView.swift` теперь явно комментирует обратное. Тот же класс находки, что уже принимался в T23. verdict: **CONFIRMED**. → **Fix now** (резолюция пользователя). **Применено**: комментарий переписан на `(BattleFightViewModel.finishBattle())`.

Обе находки добавлены как задачи `T25`/`T26` в `tasks.json`/`tasks/tracker.md` (по прецеденту T14–T24 — точечные mechanical-фиксы применены напрямую в основной сессии, без RED/GREEN-цикла: правки не меняют production-поведение, только компиляционную конфигурацию и текст комментария).

## Финальный gate (перепрогнан после фиксов T25/T26)

| Проверка | Результат |
|---|---|
| `swiftlint --quiet` | 16 нарушений, все pre-existing (тот же набор, что во всех прошлых циклах), 0 в изменённых строках |
| `xcodebuild test -scheme elf_Kit -destination 'platform=iOS Simulator,name=iPhone 17'` | **TEST SUCCEEDED** — 540/540 XCTest + 14/14 Swift Testing, 0 failures |
| `xcodebuild -scheme elf -destination '…iPhone 17' build` (Debug) | **BUILD SUCCEEDED** |
| `xcodebuild -scheme elf -destination '…iPhone 17' -configuration Release build` | **BUILD SUCCEEDED** (было `BUILD FAILED` до T25) |

## Вердикт

**PASS**

Все находки предыдущего цикла (review-2026-08-26, T21–T24) закрыты по существу — перепроверено диспетчеризованным reviewer'ом по фактическому коду, не по трекеру. Полная трассировка AC-01…AC-07 (включая AC-02b/AC-06b) проходит end-to-end без разрывов, KPI §7 достигнуты. Единственная блокирующая находка этого цикла (Release build failure, `BattleResultScreen`'s непокрытые `#Preview`-блоки) — исправлена (T25) и перепроверена повторным прогоном Release-сборки; сопутствующая non-blocking находка (устаревший комментарий, T26) — тоже исправлена в том же проходе. Debug/Release-сборки, полный unit-тест-сьют и lint — зелёные. Фича готова к `ship`.
