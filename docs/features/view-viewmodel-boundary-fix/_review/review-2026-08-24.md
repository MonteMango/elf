---
status: CHANGES REQUESTED
reviewer: "sdd:reviewer (clean-context, model: opus)"
date: "2026-08-24"
---

# Review record — view-viewmodel-boundary-fix

## Scope

Незакоммиченный рабочий diff (текущая ветка `feature/structured-task-cancellation` несёт этот фикс поверх себя, отдельной feature-ветки нет). 15 изменённых файлов (159 insertions / 39 deletions) + 12 новых файлов (unit/wiring-тесты для T1–T12 + `BattleResultCompletionViewModel.swift`).

Изменённые: `GameSession.swift`, `BattleFightViewModel.swift`, `DungeonViewModel.swift`, `FarmActivityViewModel.swift`, `GameDayViewModel.swift`, `GameDayStateViewModel.swift`, `GameSession+ViewModelFactories.swift`, `ModalRoute.swift`, `BattleFightRouteView.swift`, `BattleResultScreen.swift`, `DungeonScreen.swift`, `GameDayScreen.swift`, `PreviewGame.swift`, `.claude/docs/common-mistakes.md`, `.swiftlint.yml`.

Вне scope (принадлежат другой работе в том же рабочем дереве, не ревьюились): `CONTEXT.md`, `nextArch/*`, `Package.resolved`.

Прогнано reviewer'ом: `swiftlint --quiet` (16 pre-existing нарушений, 0 в изменённых строках), `xcodebuild test -scheme elf_Kit` (536/536 зелёных), `xcodebuild -scheme elf build` (success).

## iOS-консультанты (ADR-0002/ADR-0005)

AND-gate (spec-visible ∧ diff-visible): UI-класс и async-класс сработали (совпадает с design, см. `pipeline-log.md`); test-strategy-класс — spec-visible не сработал (упоминание XCTest в spec.md находится внутри Non-goal/negation, тот же результат, что и на design) → структурный no-op, маркер не нужен.

- **SwiftUI-консультант** — пре-консультирован на диффе четырёх View (`GameDayScreen`, `DungeonScreen`, `BattleResultScreen`, `BattleFightRouteView`) + `PreviewGame.swift` + новый `BattleResultCompletionViewModel.swift`. Обе находки вплетены в дispatch reviewer'а и подтверждены им независимо (см. ниже).
- **Swift-concurrency-консультант** — пре-консультирован на `GameSession.completeDungeonRun()`, `awaitInFlightSave()`-путях в `exitGame()`/`advanceToNextDay()`, банковке в `BattleFightViewModel.finishBattle()`, `saveInBackground()`. Находка про overclaiming-комментарий вплетена и подтверждена; остальное («TOCTOU нет», «banking→save без await», «Task не течёт») — чисто, подтверждено reviewer'ом.

## Trace-таблица US → AC → flow → task → код + тест

| AC | Flow | Task | Итог до фиксов этого прохода |
|---|---|---|---|
| AC-01 | 1 | T2, T9 | дошло; тест на "ровно один раз" слабый (см. finding #8) |
| AC-02 / AC-02b | 1 | T2, T9 | VM ✔; UI-поверхность читала `session.dungeonSession` напрямую вместо возврата VM (finding #2) |
| AC-03 | 2 | T1, T3, T4, T10, T11 | дошло; T11-тест не исполняется ни одной schema (finding #3) |
| AC-04 | 2, 3 | T1, T3, T4, T5, T12 | порядок banking vs `concludeRoomBattle` нарушен (finding #1) |
| AC-05 | 4 | T6, T7, T8 | дошло |
| AC-06 / AC-06b | 5, 6 | T7, T8 | дошло функционально; комментарий переобещает гарантию (finding #5) |
| AC-07 | — | T1–T8 | дошло (26 новых тестов, все зелёные) |
| T13 (anti-pattern doc) | — | T13 | дошло |

## Находки и резолюция

- **[stage-1] Банковка наград на смерти происходит раньше, чем `concludeRoomBattle()` добавляет награду за роковую комнату — окно потери награды** — `Packages/elf_Kit/Sources/UILayer/BattleFight/BattleFightViewModel.swift:379-390`; AC: AC-04/US-02, sad.md Flow 3 (постусловие); problem: `finishBattle()` зовёт `session.bankDungeonRewardsOnDeath()` (флашит текущий `pendingRewards` в player) ДО `battleResult = onBattleConcluded?(...)`, а именно этот launcher-коллбэк (`BattleFightRouteView.swift:30-35`) вызывает `dungeon.concludeRoomBattle(...)`, который и добавляет награду за только что зачищенную комнату в `pendingRewards` (`DungeonSession.swift:268-291`, `DefaultRoomBattleRewardMutator.swift:46-95` — по коду и собственному doc-комментарию "Rewards are *not* applied to the game here... flushed to GameSession on exit"). При победе в комнате ценой гибели героя награда за эту комнату остаётся во `pendingRewards` не флашенной; при обычном Continue её всё равно подхватит повторный `finishDungeonRun()`, но если приложение закрыто/убито между экраном смерти и Continue — награда теряется навсегда (downed-run не персистится как resumable). Нарушает обещание AC-04/US-02 "никогда не терял награды" в достижимом (хоть и не ежедневном) сценарии. Подтверждено независимо (прочитаны `finishBattle()`, `concludeRoomBattle()`, `DefaultRoomBattleRewardMutator`, `bankDungeonRewardsOnDeath`). verdict: **CONFIRMED**. → **Fix now.**
- **[stage-1] Тест T5 структурно не может поймать этот порядок** — `Packages/elf_Kit/Tests/elf_KitTests/UILayer/BattleFight/BattleFightViewModel_DeathRewardBankingTests.swift:146,181` (`onBattleConcluded: nil`); AC: AC-04; problem: обе проверки гоняют `finishBattle()` без launcher'а, `concludeRoomBattle` вообще не участвует — тест подтверждает только внутренний порядок двух вызовов VM, не бизнес-исход. → **Fix now** (вместе с находкой выше, один follow-up task).
- **[stage-1] AC-02/AC-02b на UI-поверхности не обеспечены — навигация решается по состоянию session, а не по результату VM** — `Packages/elf_iOS/Sources/Screens/GameDayScreen/GameDayScreen.swift:129-137`; AC: AC-02, AC-02b, sad.md Flow 1 (явно рисует `nil` no-op возврат); problem: `startDungeonRun()` возвращает `Void`, View навигирует по любому непустому `session.dungeonSession` — воспроизводит связанность View↔`GameSession`-shape (независимо найдено SwiftUI-консультантом), в комбинации с уже открытым в spec §8 вопросом "нет защиты от второго старта" может увести Игрока в чужой/старый забег вместо no-op. verdict: **CONFIRMED** (код подтверждён; конкретный сценарий провала завязан на уже отдельно открытый вопрос §8, но сама связанность View↔session — самостоятельный дефект). → **Fix now.**
- **[stage-1] Единственный тест T11 не исполняется ни одной схемой проекта** — `Packages/elf_iOS/Tests/elf_iOSTests/BattleResultScreen_CompanionViewModelTests.swift`; AC: AC-03/AC-04/AC-07 (T11); problem: `xcodebuild -list -project elf.xcodeproj` даёт схемы только `elf`/`elf_Kit`; у `elf_iOS` нет собственной shared-схемы, TestAction схемы `elf` включает только `elfTests`/`elfUITests` — документированная в CLAUDE.md команда `xcodebuild test -scheme elf_Kit` этот файл не запускает. Подтверждено (`xcodebuild -list` перепроверен). verdict: **CONFIRMED**. → **Defer** — записано в spec §8 как отдельная задача тест-инфраструктуры (elf_iOS сейчас вообще не имеет прогоняемой schema для юнит-тестов — шире узкого scope этого XS-фикса).
- **[stage-2] `prepareDungeonRun()` осиротел и дублирует логику выбора отряда** — `Packages/elf_Kit/Sources/UILayer/GameDay/GameDayViewModel.swift:78-90` vs `95-109`; problem: ни одного продакшн-вызова не осталось (grep по репо), тело почти дословно повторено внутри `startDungeonRun()`. verdict: **CONFIRMED**. → **Fix now.**
- **[stage-2] Комментарии переобещают гарантию `awaitInFlightSave()`** — `GameDayViewModel.swift:193-194`, `GameDayStateViewModel.swift:89-90`; problem: комментарий "so the two saves never interleave" сильнее, чем факт — `awaitInFlightSave()` дренирует только уже идущий на момент вызова save, последующий `await session.save()` сам не регистрируется в `saveInFlight`; sad.md Flow 5 честно фиксирует эту оговорку, так что для AC это не пробел, а расхождение комментария с уже принятым в SAD ограничением. verdict: **CONFIRMED**. → **Fix now** (переформулировать в терминах sad.md).
- **[stage-2] Hunt-бой инициирует лишний проход фонового сохранения** — `BattleFightViewModel.swift:383` + `GameSession.swift` (`concludeHuntBattle` уже сам зовёт `saveInBackground()`); problem: безусловный `session.saveInBackground()` в `finishBattle()` + внутренний вызов в `concludeHuntBattle()` — коалесцируется собственным механизмом `saveInBackground()`, не гонка, но лишний follow-up проход записи на диск на каждый hunt-бой. verdict: **CONFIRMED**. → **Fix now** (ограничить безусловный вызов dungeon-путём).
- **[stage-2] `BattleResultScreen` без session рендерится пустым** — `ModalRoute.swift:46` + `SessionRouteView.swift`; problem: `SessionRouteView.body` без `else`-ветки на случай уже освобождённых `gameSession`/`dayStateViewModel` — регресс относительно прежнего поведения с опциональной session; низкая достижимость (offline single-player). verdict: **CONFIRMED**. → **Fix now** (fallback `router.dismissModal()`, по аналогии со stale-route).
- **[stage-2] Тест "AP списывается ровно один раз" не различает двойное списание** — `Packages/elf_Kit/Tests/elf_KitTests/UILayer/GameDay/GameDayViewModel_StartDungeonRunTests.swift:104-114`; problem: фикстура `current:100, maximum:100` при `dungeonCost=100` — `ActionPoints.spend`'s guard сам отклоняет второе списание, поэтому 1 и 2 списания дают одинаковый результат. verdict: **CONFIRMED**. → **Fix now** (`current:250`, ассерт `150`).
- **[stage-2, info, без действия по решению reviewer'а] Порядок `spendActionPoints`/`startDungeonSession` в коде обратный относительно sad.md Flow 1** — `GameDayViewModel.swift:107-108`; обе мутации синхронные и независимые, расхождение безобидное (reviewer: "ничего менять не нужно"). Не выносилось на резолюцию.

## Вердикт

**CHANGES REQUESTED** — 7 находок отправлены на `implement` (fix now), 1 находка (T11 test-инфраструктура) отложена в spec §8. После фиксов — повторное ревью изменённой поверхности (`finishBattle()`/launcher-путь, `GameDayScreen`/`startDungeonRun()`, `prepareDungeonRun()`, комментарии, hunt-save, `SessionRouteView`, AC-01 тест).
