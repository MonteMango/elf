# Review record — architecture-hardening (2026-07-11)

## Scope

Independent, clean-context review of the whole `architecture-hardening` diff — 20 tasks (T1–T20), all marked `done` in `tasks/tracker.md`, gate reported GREEN after a 2026-07-10 remediation pass.

Per the project's `CLAUDE.md` Git Policy, nothing on this feature is committed — the entire implementation is uncommitted working-tree state on `main`. Diff scope:

- **Modified (tracked):** 25 files, +469/−410 (`.swiftlint.yml`, `CLAUDE.md`, `GameSession.swift`, `DungeonSession.swift`, `GameStore.swift`, `BattleFightViewModel.swift`, `CharacterCreationViewModel.swift`, `GameDayViewModel.swift`, logging trio, `AppRoute.swift`, `AppCoordinator.swift`, `SessionRouteView.swift`, 6 Screens, `elf_iOS/Package.swift`, `docs/architecture-map.md`, `sad.md`).
- **New (untracked):** 9 new `DataLayer/Services/*` mutator/service directories, ~13 new test files under `elf_KitTests`/`elf_iOSTests`, plus the feature's own SDD artifacts (`spec.md`, `sad.md`, `data-model.md`, `tasks.json`, `tasks/`, `adr/`, `contracts/`, `test-plan.md`).

Dispatched 3 independent read-only `sdd:reviewer` passes (fan-out, per the L-size / full-route review-dimensions guidance):

1. **Stage-1 general AC/US trace** — all ACs except AC-06.
2. **Stage-1 AC-06 deep dive** — "delegation is real" across all 11 extractions (T7–T17) + the 3 orchestrator-convergence checks (T12/T14/T17).
3. **Stage-2 quality** — conventions, DI, boundaries, error/edge handling, test adequacy, SwiftLint config, `elf_iOS` test target wiring.

## Findings + resolutions

All findings below were resolved with the user (owner: Vitalii Lytvynov) via `AskUserQuestion`, one at a time, with full context.

### Stage 1 (AC compliance — blocking until resolved)

| # | Finding | File:line | AC | Resolution |
|---|---|---|---|---|
| 1 | `BattleFightViewModel.rescaleCurrentVitals`/`scaledVital` still perform inline domain-rule mutation (proportional HP/MP rescale on buff-cap shift) — not delegated to any extracted mutator; `scaledVital` is also declared `static`, violating the repo's "never use `static`" rule. | `BattleFightViewModel.swift:207-233` | AC-06 | **Fix now** — extract into a DI-injected mutator (protocol + `Default*` + `+Dependency.swift`, own unit test) and drop `static`. Hands off to `implement` as a follow-up task. |
| 2 | AC-04 invariant #1 regression test (`RewardApplicationMutatorTests.swift:71`) is near-tautological: the extraction turned `concludeHuntBattle` into a pure pass-through, so the ordering is now structurally guaranteed by data-flow and the test cannot fail on a realistic reordering. | `RewardApplicationMutatorTests.swift:71` | AC-04 | **Fix now** — add a facade-level test driving `concludeHuntBattle` through the real mutator + real calculator, asserting the overlay's `previousExp` equals the starting exp. Hands off to `implement`. |
| 3 | AC-03 claims UI-layer mutation of session state "does not compile," but `GameStore.player` (`GameStore.swift:44`) is `public var` (settable) — `session.state.player.currentExp += n` from `elf_iOS` compiles, unlike the correctly `public internal(set)` fields (`currentDay`/`calendar`/`houses`/`actionPoints`). Pre-existing, not a regression from this diff (the diff only touched the doc comment above it, one of the two AC-08 fixes). | `GameStore.swift:44` | AC-03 | **Fix now** — tighten `player` to `public internal(set)` with an internal write path through `GameSession`; verify no `elf_iOS` call site currently writes through it directly. Hands off to `implement`. |
| 4 | Working tree bundles unrelated edits — a new "## Git Policy" section in `CLAUDE.md` and an `architecture-map.md` reconciliation-note update/`reflects_commit` bump — outside AC-08's two named items (the `GameStore.swift` stale comment + the iOS 17→18 platform line, both confirmed correctly applied). Verified via `git log`: HEAD's committed `CLAUDE.md` has no Git Policy section, so this is real uncommitted content, not a review artifact. | `CLAUDE.md`, `docs/architecture-map.md` | AC-08 | **Not an issue** — confirmed unrelated to `architecture-hardening`; leftover uncommitted state from earlier, separate sessions sharing the same never-commit working tree. Left as-is, out of this feature's diff attribution. |
| 5 | `sad.md` §11 self-flags an open question: AC-04 invariant #2 (world-turn roster-reshuffle guard) has no §6 sequence flow — §6 jumps from flow 2 to flow 4. The invariant itself is soundly covered by `WorldTurnMutatorTests` (genuinely fails if the guard is dropped) — this is a documentation-completeness gap only. | `sad.md` §6 / §11 | AC-04 | **Deferred** — added to `spec.md` §8 Open Questions (owner: Vitalii Lytvynov, due: next `sad.md` revision for this feature). Does not block ship; the invariant is test-protected. |

**All other in-scope stage-1 items — AC-01, AC-02, AC-05, AC-07, AC-09, and all 11 AC-06 extractions except finding #1 above (T7–T17 incl. the 3 convergence checks), and the full US-01..US-07 → AC mapping — came back clean** from the two stage-1 reviewer passes: build/test/lint spot-checks were internally consistent with `tracker.md`'s claimed GREEN gate; the 2 originally `#if DEBUG`-gated print sites kept their guards; the `raw_print_banned` SwiftLint rule + allow-list are present and correctly scoped; `.gameSession`→`GameID` / `.calendar`→zero-payload both drop hand-written equality correctly with `SessionRouteView` popping back on a stale `GameID`; DUP-1's 3 shims preserve their signatures over one core path; all 11 mutator extractions are genuine separate injected types (no `extension`-based fakes, no `static`/singleton mutators) with independent, non-tautological unit tests and single-delegating-call facade methods.

### Stage-1 — reported but judged non-blocking

| # | Finding | File:line | AC | Resolution |
|---|---|---|---|---|
| 6 | Lower-confidence AC-06 residuals in `GameSession`: `advanceToNextDay` keeps the next-`GameDay` lookup inline alongside the delegated houses-logic; `spendActionPoints` performs an inline read-modify-write via `ActionPoints.spend()`. | `GameSession.swift:88-95`, `104-110` | AC-06 | **Not an issue** — judged thin facade orchestration (store-pointer navigation; the spend invariant is carried by the `ActionPoints` value type, not the facade), not an inline domain rule in AC-06's sense. |

### Stage 2 (quality — non-blocking, but genuinely worth fixing)

| # | Finding | File:line | Resolution |
|---|---|---|---|
| 7 | Logging severity/gating inconsistency from the AC-02 print→logger migration: `CharacterCreationViewModel.swift:222` routes a *success* event through `logError`; `GameDayViewModel.swift:96,107,179` route plain UI traces through it too — `ConsoleDebugGameLogger.logError` prints all of them with a `⚠️` prefix. Additionally, `DebugGameLogger.swift`'s documented "caller wraps in `#if DEBUG`" contract holds for the 2 old persistence sites but not the 7 new VM sites, and `logError` itself has no `#if DEBUG`/category guard (unlike sibling methods, e.g. `logWorldTurn`). | `DebugGameLogger.swift`, `ConsoleDebugGameLogger.swift`, `CharacterCreationViewModel.swift`, `GameDayViewModel.swift` | **Fix now** — add `logDebug`/`logInfo` to `DebugGameLogger` for non-error traces, move the success/UI-trace call sites onto it, and pick one consistent `#if DEBUG`/category guard (call-site or sink) and align the doc comment. Hands off to `implement`. |
| 8 | Two untracked review-source docs (`architecture-review.md`, `architecture-review-summary.md`) sat at the repo root instead of `docs/features/architecture-hardening/_audit/`, where the rest of this feature's audit material lives. | repo root | **Fixed directly** (housekeeping, no code/tests involved) — moved both files into `docs/features/architecture-hardening/_audit/`. |

**Everything else stage-2 came back clean:** every new mutator follows the repo's `protocol + Default* + +Dependency.swift` pattern, DI keys wired with `testValue { liveValue }`, no `static`, no new force-unwraps, `@MainActor` vs `Sendable` correctly discriminated per mutator's state-touching needs, new tests assert real observable state (not mock-was-called tautologies) with genuine edge coverage, `raw_print_banned` SwiftLint rule is well-scoped, and the `elf_iOS` test target mirrors `elf_Kit`'s pattern.

## Gate result

**CHANGES REQUESTED** — 4 stage-1/stage-2 findings resolved as "Fix now" (#1, #2, #3, #7) still need code changes; they are handed off to `implement` as follow-up work, re-entering the TDD loop per finding. 1 finding deferred to `spec.md` §8 (#5, doc-only, non-blocking). 1 housekeeping finding fixed directly (#8). 2 findings dismissed with recorded rationale (#4, #6).

## Follow-up resolution (2026-07-11)

Findings #1, #2, #3 and #7 were implemented as tasks T21–T24 (see `tasks/tracker.md` §"T21–T24 — review follow-up fixes") through the full RED→GREEN→GATE TDD cycle. Ground truth after: build succeeded, 505/505 unit tests pass, lint unchanged at 16 pre-existing/out-of-scope violations. A re-review of the whole diff (`/sdd:review architecture-hardening`) is the next step before shipping.

## Re-review (2026-07-11)

Independent, clean-context re-review of the whole diff post-T21–T24, dispatched as 2 parallel `sdd:reviewer` passes:

1. **T21–T24 fix verification** — independently re-checked each of the 4 prior "Fix now" findings against the actual current code (not `tracker.md`'s self-report).
2. **Full stage-1 AC/US trace + stage-2 quality** — re-ran the whole §4 US set + §5 AC set end-to-end trace (spec → sad.md §6 → tasks.json → code + test) plus stage-2 quality, per the review protocol's backstop requirement (does not just trust the diff's claimed ACs).

### T21–T24 verification results

All 4 findings verified **RESOLVED** by independent code read (not by trusting `tracker.md`):

- **#1** (`VitalsRescaleMutator` extraction, drop `static`) — genuine separate injected triad under `Services/VitalsRescale/`, own unit test (8 relocated edge cases + 1 new), `scaledVital` no longer `static` anywhere, delegation proven via sentinel-value spy test. The deleted `BattleFightViewModelTests.swift` contained only the 8 relocated cases — no coverage lost.
- **#2** (AC-04 invariant #1 non-tautological test) — `GameSession_RewardApplicationDelegationTests.swift:209` drives `concludeHuntBattle` through the real `DefaultRewardApplicationMutator` + real `DefaultBattleResultCalculator`; reasoned through the production read-before-mutate path (`GameSession.swift:182` vs `:185`) and confirmed the test would genuinely fail if that ordering were reversed.
- **#3** (`GameStore.player` access) — now `public internal(set) var player` (`GameStore.swift:50`), no `elf_iOS` write call site, source-content pin test present.
- **#7** (logging severity/gating) — `logDebug(_:)` added to both sinks, named call sites moved off `logError`, `#if DEBUG` now lives sink-side only (confirmed removed from both named call sites), `LoggerRoutingTests` extended.

No regressions or new issues introduced by T21–T24.

### New findings from the full-trace pass

| # | Finding | File:line | AC | Resolution |
|---|---|---|---|---|
| 9 | AC-05 (navigation route resolution) had zero automated test coverage — `SessionRouteView.swift:33-43`'s match/mismatch + pop-back logic (the highest-risk *new* runtime behavior in this feature: a stale `GameID` silently pops back instead of crashing) was exercised only by a manual navigation pass. `test-plan.md` explicitly requires Component + E2E-through-UI tests for this; `AppRouteTests.swift` covers only AC-07 (Hashable/Equatable synthesis), not AC-05's resolve/pop-back behavior. Confirmed independently by both reviewer passes and by direct code read. | `SessionRouteView.swift:33-43`, `AppRoute.swift:124-129` | AC-05 | **Fix now** — added as task **T25** in `tasks.json`/`tasks/tracker.md`: extract the match/mismatch decision into a pure, unit-testable helper (no SwiftUI view-hosting harness exists in `elf_iOS/Tests` today) and test match→render, mismatch→pop, and `.calendar` resolve-from-session. Hands off to `implement`. |
| 10 | AC-07's wording ("hand-written equality gone for both converted cases") and the §7 KPI ("target: 0") don't literally hold — `AppRoute.swift:44,51-52,74,77-78` still carry manual `==`/`hash(into:)` branches for `.gameSession`/`.calendar`, because the enum's `Equatable`/`Hashable` conformance stays hand-written overall (battle routes carry `Battle`, which can't synthesize) — full removal is structurally infeasible while battle routes stay out of scope, not an oversight. The *observable* de-dup behavior is correct and tested (`AppRouteTests.swift`). | `spec.md` AC-07, §7 KPI | AC-07 | **Not an issue, wording corrected** — code accepted as-is (structurally required); `spec.md` AC-07 and the §7 KPI line edited to say each converted case's branch collapses to one identity/always-true comparison (not full removal), matching reality. |

Everything else re-checked clean: full US-01..US-07 → AC-01..AC-09 trace holds (AC-04 §2's known, already-deferred `sad.md` §6 doc gap re-confirmed non-blocking); AC-06 spot-checked across `RewardApplication`/`WorldTurn`/`VitalsRescale`/`DuelPairing`/`RoundExecution`/`RoomBattleReward` — each a genuine DI triad, no `static`/singleton/extension-fake, independent test, one-line facade delegation; `swiftlint --strict` still 16 pre-existing/out-of-scope violations, `raw_print_banned` at 0 hits; AC-01/02/03/06/08/09 hold.

## Gate result (re-review)

**CHANGES REQUESTED** — one open stage-1 finding (#9, AC-05 test coverage) remains, handed off as task **T25**. Finding #10 resolved directly via a `spec.md` wording correction (no code change). Once T25 lands (RED→GREEN→GATE) and the changed surface (`SessionRouteView.swift` + its new test) is re-reviewed, this feature is ready for `/sdd:ship`.

## Round 3 — T25 verification (2026-07-11)

Independent, clean-context `sdd:reviewer` pass dispatched specifically over the changed surface since round 2: `SessionRouteView.swift`'s extracted `sessionMatchesExpectedGameId` helper + the new `SessionRouteViewTests.swift`, plus finding #10's `spec.md` wording correction, plus a full-feature build/test sanity pass.

- **Finding #9 (AC-05 coverage) — verified genuinely resolved, not by trusting `tracker.md`.** `sessionMatchesExpectedGameId(sessionGameId:expectedGameId:)` (`SessionRouteView.swift:49-51`) is a pure `internal` free function, and production `body` calls that exact function to gate render-vs-pop (`SessionRouteView.swift:35`) — the tested unit is the production decision, not a copy that could drift. `SessionRouteViewTests.swift` drives it with real `elf_Kit` `GameID` values through `@testable import elf_iOS`: matching id → render, two distinct `GameID()` → pop-back, `nil` expectedGameId → always-match (`.calendar`) — non-tautological, would fail if the `||`/`==` logic were inverted or dropped. Independently re-ran `cd Packages/elf_iOS && xcodebuild test -scheme elf_iOS -destination 'platform=iOS Simulator,name=iPhone 17'` → **6/6 pass, 0 failures**, matching the tracker's claim. Accepted non-blocking residual (already in T25's agreed scope, documented in the test file's header): the pop-back *action* (`Color.clear.task { router.pop() }`) itself stays untested for lack of a SwiftUI view-hosting harness — 2 trivially-readable lines directly consuming the tested function, negligible divergence risk, not a new finding.
- **Finding #10 (spec wording) — verified accurate against code.** `spec.md` AC-07 and the §7 KPI line now correctly describe each converted case's branch as collapsing to one identity/always-true comparison, not full removal. Cross-checked against `AppRoute.swift`: `.gameSession` `==` collapses to `lhsId == rhsId` (`:51-52`), `.calendar` folds into the always-true group `return true` (`:44,50`), both `hash(into:)` branches drop per-field logic (`:74-76,77-78`).
- **Full-feature sanity pass** — `git diff --stat` shows only the T25 surface (`SessionRouteView.swift`, new `Packages/elf_iOS/Tests/`, `Package.swift` test-target wiring) and the `spec.md` wording fix added beyond round 2; nothing else crept in. Independently re-ran `xcodebuild -scheme elf -destination 'platform=iOS Simulator,name=iPhone 17' build` → **BUILD SUCCEEDED**, and `xcodebuild test -scheme elf_Kit -destination 'platform=iOS Simulator,name=iPhone 17'` → **505/505 pass, 0 failures**, both reproducing the tracker's claimed ground truth independently rather than trusting it.

**No new findings.**

## Gate result (round 3 — final)

**PASS.** All 10 findings across all three rounds are resolved (fixed / deferred-with-owner / dismissed-with-reason); the one open stage-1 item (#9) is now closed by T25 with independently-verified, non-tautological test coverage. Build, `elf_Kit` tests (505/505), and `elf_iOS` tests (6/6) all independently reproduced green. `architecture-hardening` is ready for `/sdd:ship`.
